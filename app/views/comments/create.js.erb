$('.comment-errors').html('<%= render 'shared/errors', resource: @comment %>');

<% if current_user.present? %>
  $('.comments').append('<%= j render @comment %>');
  $('.new-comment #comment_body').val('');
<% end %>